/** Initializes a new Square game and starts running it. */
class Main {
    function void main() {
        var MainGame game;
        let game = MainGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
